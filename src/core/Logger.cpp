#include "Logger.hpp"

namespace mcgan {
namespace core {

// Logger class is fully implemented in the header file with inline methods.
// The static members are also initialized inline in the header.
// 
// This file serves as a placeholder for any future non-inline implementations.

// If you need to add utility functions or additional logging features, add them here:

/*
// Example: Helper function to configure logger from config file
void configureLoggerFromFile(Logger& logger, const std::string& config_file) {
    // Parse config and set logger properties
    logger.setLevel(LogLevel::INFO);
    logger.enableFileOutput("app.log");
}
*/

} // namespace core
} // namespace mcgan